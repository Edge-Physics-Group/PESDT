[build-system]
requires = [
    "setuptools>=61",
    "wheel",
    "cherab==1.5.0.1",
    "raysect>=0.9.1",
    "Cython>=3.0",
    "numpy>=2.0"
]
build-backend = "setuptools.build_meta"

[project]
name = "cherab-PESDT-addon"
version = "2.0.dev"
dependencies = [

]